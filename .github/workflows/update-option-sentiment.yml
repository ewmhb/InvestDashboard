name: Update option sentiment data

on:
  workflow_dispatch:
  schedule:
    - cron: "15 23 * * 1-5"

permissions:
  contents: write

concurrency:
  group: update-option-sentiment
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Download Cboe option sentiment data
        run: python update_options_sentiment.py
      - name: Commit updated option sentiment data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add options_sentiment.json
          git diff --cached --quiet || git commit -m "Update option sentiment data"
          git push
